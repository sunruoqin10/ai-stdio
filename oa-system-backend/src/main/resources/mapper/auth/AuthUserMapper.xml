<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.oa_system_backend.module.auth.mapper.AuthUserMapper">

    <select id="selectByUsernameOrEmailOrMobile" resultType="com.example.oa_system_backend.module.auth.entity.AuthUser">
        SELECT * FROM auth_user
        WHERE (username = #{username} OR email = #{email} OR mobile = #{mobile})
          AND is_deleted = 0
    </select>

    <update id="updateLoginStats">
        UPDATE auth_user
        SET last_login_time = #{lastLoginTime},
            last_login_ip = #{lastLoginIp},
            login_attempts = #{loginAttempts},
            status = #{status},
            locked_until = #{lockedUntil},
            updated_at = NOW()
        WHERE id = #{userId}
    </update>

</mapper>
