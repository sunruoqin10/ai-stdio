<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.oa_system_backend.module.dict.mapper.DictTypeMapper">

    <select id="selectPageByQuery" resultType="com.example.oa_system_backend.module.dict.entity.DictType">
        SELECT * FROM sys_dict_type
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (code LIKE CONCAT('%', #{keyword}, '%')
                 OR name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY sort_order, id
    </select>

    <select id="countByCode" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM sys_dict_type
        WHERE code = #{code}
        AND is_deleted = 0
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>

    <select id="selectByCode" resultType="com.example.oa_system_backend.module.dict.entity.DictType">
        SELECT * FROM sys_dict_type
        WHERE code = #{code}
        AND is_deleted = 0
    </select>

    <update id="updateItemCount">
        UPDATE sys_dict_type
        SET item_count = (
            SELECT COUNT(*) FROM sys_dict_item
            WHERE dict_type_id = #{dictTypeId}
            AND is_deleted = 0
        )
        WHERE id = #{dictTypeId}
    </update>

    <select id="selectAll" resultType="com.example.oa_system_backend.module.dict.entity.DictType">
        SELECT * FROM sys_dict_type
        WHERE is_deleted = 0
        ORDER BY sort_order, id
    </select>

</mapper>